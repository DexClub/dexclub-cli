name: Build CLI Package

on:
  workflow_call:
    inputs:
      native_target:
        description: "仅构建指定 DexKit native 目标；留空或 all 时构建全部"
        required: false
        type: string
        default: all
      release_version:
        description: "正式发行版本；非 tag 构建留空"
        required: false
        type: string
        default: ""
      release_commit:
        description: "构建对应的完整 Git commit SHA"
        required: true
        type: string
      release_dirty:
        description: "构建输入是否包含未提交修改"
        required: false
        type: boolean
        default: false

jobs:
  build-cli-package:
    name: build-cli-package (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-x64
            runner: ubuntu-24.04
            os_name: linux
            arch: x64
            dist_task: :cli-app:shadowDistZip
            msys2_msystem: ""
            msys2_install: ""
            msys2_bin_subdir: ""
          - target: linux-arm64
            runner: ubuntu-24.04-arm
            os_name: linux
            arch: arm64
            dist_task: :cli-app:shadowDistZip
            msys2_msystem: ""
            msys2_install: ""
            msys2_bin_subdir: ""
          - target: android-arm64
            runner: ubuntu-24.04
            os_name: android
            arch: arm64
            dist_task: :cli-app:shadowDistZip
            msys2_msystem: ""
            msys2_install: ""
            msys2_bin_subdir: ""
          - target: windows-x64
            runner: windows-2022
            os_name: windows
            arch: x64
            dist_task: :cli-app:shadowDistZip
            msys2_msystem: MINGW64
            msys2_install: >-
              mingw-w64-x86_64-gcc
              mingw-w64-x86_64-cmake
              mingw-w64-x86_64-ninja
              mingw-w64-x86_64-zlib
            msys2_bin_subdir: mingw64/bin
          - target: macos-x64
            runner: macos-15-intel
            os_name: macos
            arch: x64
            dist_task: :cli-app:shadowDistZip
            msys2_msystem: ""
            msys2_install: ""
            msys2_bin_subdir: ""
          - target: macos-arm64
            runner: macos-15
            os_name: macos
            arch: arm64
            dist_task: :cli-app:shadowDistZip
            msys2_msystem: ""
            msys2_install: ""
            msys2_bin_subdir: ""

    steps:
      - name: Select target
        id: filter
        shell: pwsh
        run: |
          $selectedTarget = "${{ inputs.native_target }}"
          $enabled = [string]::IsNullOrWhiteSpace($selectedTarget) -or
            $selectedTarget -eq "all" -or
            $selectedTarget -eq "${{ matrix.target }}"

          if ($enabled) {
            "enabled=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          } else {
            "enabled=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
            Write-Host "Skip matrix target: ${{ matrix.target }}"
          }

      - name: Checkout
        if: steps.filter.outputs.enabled == 'true'
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Verify build metadata inputs
        if: steps.filter.outputs.enabled == 'true'
        shell: pwsh
        env:
          DEXCLUB_RELEASE_COMMIT: ${{ inputs.release_commit }}
        run: |
          $actualCommit = (git rev-parse HEAD).Trim()
          if ($actualCommit -ne $env:DEXCLUB_RELEASE_COMMIT) {
            throw "Checked out commit $actualCommit does not match release_commit."
          }
          if (git status --porcelain) {
            throw "Checkout must be clean before build metadata is resolved."
          }

      - name: Setup Java
        if: steps.filter.outputs.enabled == 'true'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Gradle
        if: steps.filter.outputs.enabled == 'true'
        uses: gradle/actions/setup-gradle@v4

      - name: Setup Android SDK
        if: steps.filter.outputs.enabled == 'true'
        uses: android-actions/setup-android@v4
        with:
          packages: >-
            platform-tools
            build-tools;36.0.0
            platforms;android-36
            ndk;28.2.13676358
            cmake;3.18.1
            cmake;3.31.6

      - name: Setup MSYS2 toolchain
        if: steps.filter.outputs.enabled == 'true' && runner.os == 'Windows'
        id: msys2
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msys2_msystem }}
          update: true
          path-type: minimal
          install: ${{ matrix.msys2_install }}

      - name: Prepare Android local.properties
        if: steps.filter.outputs.enabled == 'true'
        shell: pwsh
        run: |
          $sdkDir = $env:ANDROID_SDK_ROOT
          if ([string]::IsNullOrWhiteSpace($sdkDir)) {
            throw "ANDROID_SDK_ROOT is empty after setup-android"
          }

          $sdkDir = $sdkDir -replace '\\', '/'
          "sdk.dir=$sdkDir" | Out-File -FilePath (Join-Path $PWD "local.properties") -Encoding ascii
          "sdk.dir=$sdkDir" | Out-File -FilePath (Join-Path $PWD "dexkit-binding/vendor/DexKit/local.properties") -Encoding ascii

      - name: Download native artifact
        if: steps.filter.outputs.enabled == 'true'
        uses: actions/download-artifact@v4
        with:
          name: dexkit-native-${{ matrix.target }}
          path: .ci/native

      - name: Build CLI shadow distribution
        if: steps.filter.outputs.enabled == 'true'
        shell: pwsh
        env:
          DEXCLUB_RELEASE_VERSION: ${{ inputs.release_version }}
          DEXCLUB_RELEASE_COMMIT: ${{ inputs.release_commit }}
          DEXCLUB_RELEASE_DIRTY: ${{ inputs.release_dirty }}
        run: |
          $nativeDir = (Resolve-Path (Join-Path $PWD ".ci/native")).Path
          $gradleArgs = @(
            "--no-daemon"
            "-Pdexkit.native.dir=$nativeDir"
            "-PreleaseCommit=$env:DEXCLUB_RELEASE_COMMIT"
            "-PreleaseDirty=$env:DEXCLUB_RELEASE_DIRTY"
          )
          if (![string]::IsNullOrWhiteSpace($env:DEXCLUB_RELEASE_VERSION)) {
            $gradleArgs += "-PreleaseVersion=$env:DEXCLUB_RELEASE_VERSION"
          }
          $gradleArgs += "${{ matrix.dist_task }}"

          if ($env:RUNNER_OS -eq 'Windows') {
            $msys2Location = "${{ steps.msys2.outputs.msys2-location }}"
            if ([string]::IsNullOrWhiteSpace($msys2Location)) {
              throw "MSYS2 location output is empty on Windows build."
            }

            $toolchainBin = Join-Path $msys2Location "${{ matrix.msys2_bin_subdir }}"
            if (!(Test-Path $toolchainBin)) {
              throw "Missing MSYS2 toolchain bin directory: $toolchainBin"
            }

            $env:PATH = "$toolchainBin;$env:PATH"
            & ".\\gradlew.bat" @gradleArgs
          } else {
            & "./gradlew" @gradleArgs
          }

      - name: Package artifact
        if: steps.filter.outputs.enabled == 'true'
        shell: pwsh
        run: |
          $distDir = Join-Path $PWD "cli-app/build/distributions"
          $sourceZips = @(Get-ChildItem -Path $distDir -Filter 'cli-app-shadow-*.zip' -File)
          if ($sourceZips.Count -ne 1) {
            throw "Expected one versioned CLI distribution archive, found $($sourceZips.Count)."
          }
          $sourceZip = $sourceZips[0].FullName

          $artifactDir = Join-Path $PWD "artifacts"
          $packageDir = Join-Path $PWD ".ci/package"
          $extractDir = Join-Path $PWD ".ci/extract/cli"
          $licensePath = Join-Path $PWD "LICENSE"
          if (!(Test-Path $licensePath)) {
            throw "Missing LICENSE file: $licensePath"
          }

          if (Test-Path $packageDir) {
            Remove-Item -Recurse -Force $packageDir
          }
          if (Test-Path $extractDir) {
            Remove-Item -Recurse -Force $extractDir
          }
          New-Item -ItemType Directory -Force -Path $artifactDir | Out-Null
          New-Item -ItemType Directory -Force -Path $packageDir | Out-Null
          New-Item -ItemType Directory -Force -Path $extractDir | Out-Null

          $targetBaseName = "dexclub-cli-${{ matrix.os_name }}-${{ matrix.arch }}"
          $targetZip = Join-Path $artifactDir "$targetBaseName.zip"
          Expand-Archive -Path $sourceZip -DestinationPath $extractDir -Force

          $rootEntries = Get-ChildItem -Path $extractDir
          if ($rootEntries.Count -ne 1 -or -not $rootEntries[0].PSIsContainer) {
            throw "Expected a single root directory inside CLI distribution archive."
          }

          $sourceRoot = $rootEntries[0].FullName
          Copy-Item -Path (Join-Path $sourceRoot "*") -Destination $packageDir -Recurse -Force
          if (!(Test-Path (Join-Path $packageDir "VERSION"))) {
            throw "CLI distribution does not contain generated VERSION metadata."
          }
          Copy-Item -Path $licensePath -Destination (Join-Path $packageDir "LICENSE") -Force

          if (Test-Path $targetZip) {
            Remove-Item -Force $targetZip
          }
          Compress-Archive -Path (Join-Path $packageDir "*") -DestinationPath $targetZip

          $hash = (Get-FileHash -Path $targetZip -Algorithm SHA256).Hash.ToLowerInvariant()
          "$hash  $targetBaseName.zip" | Out-File -FilePath (Join-Path $artifactDir "$targetBaseName.sha256") -Encoding ascii

      - name: Upload package artifact
        if: steps.filter.outputs.enabled == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: dexclub-cli-${{ matrix.os_name }}-${{ matrix.arch }}
          path: artifacts/
          if-no-files-found: error
